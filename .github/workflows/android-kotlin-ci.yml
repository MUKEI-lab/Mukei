name: android-kotlin-ci

on:
  push:
    branches: [ Kotlin, temp/universal-storage-workspace-v0.1 ]
    paths:
      - "android/**"
      - "rust/Cargo.toml"
      - "rust/crates/mukei-core/**"
      - "rust/crates/mukei-android-jni/**"
      - "rust/migrations/**"
      - "rust/llama-cpp-prebuilt/**"
      - ".github/workflows/android-kotlin-ci.yml"
      - ".github/workflows/android-release-candidate.yml"
  pull_request:
    branches: [ Kotlin, temp/universal-storage-workspace-v0.1 ]
    paths:
      - "android/**"
      - "rust/Cargo.toml"
      - "rust/crates/mukei-core/**"
      - "rust/crates/mukei-android-jni/**"
      - "rust/migrations/**"
      - "rust/llama-cpp-prebuilt/**"
      - ".github/workflows/android-kotlin-ci.yml"
      - ".github/workflows/android-release-candidate.yml"
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

concurrency:
  group: android-kotlin-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  RUST_TOOLCHAIN: "1.86.0"
  ANDROID_NDK_VERSION: "27.2.12479018"
  CARGO_NDK_VERSION: "4.1.2"

jobs:
  publish-pending:
    runs-on: ubuntu-latest
    steps:
      - name: Publish pending Android CI status
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: 'pending',
              context: 'android-kotlin-ci',
              description: 'Android and native validation is running',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });

  rust-security:
    needs: publish-pending
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.86.0
      - name: Install native build dependencies
        run: >-
          sudo apt-get update &&
          sudo apt-get install -y clang cmake ninja-build pkg-config libssl-dev perl make
      - name: Test platform-neutral runtime
        working-directory: rust
        run: cargo test -p mukei-core --lib
      - name: Test canonical prompt sandbox
        working-directory: rust
        run: cargo test -p mukei-core tools::
      - name: Check SQLCipher core
        working-directory: rust
        run: >-
          cargo check -p mukei-core
          --no-default-features
          --features std,tokio,sqlcipher,runtime_hardening
      - name: Test secure JNI bootstrap contracts
        working-directory: rust
        run: >-
          cargo test -p mukei-android-jni --lib
          --no-default-features
          --features secure_runtime,runtime_hardening
      - name: Check production RAG composition
        working-directory: rust
        run: >-
          cargo check -p mukei-android-jni
          --no-default-features
          --features secure_runtime,rag_runtime,runtime_hardening
      - name: Build and test host inference capsule
        run: |
          set -euo pipefail
          cmake -S rust/llama-cpp-prebuilt \
            -B build/llama-host \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DMUKEI_LLAMA_FORCE_REBUILD=ON
          cmake --build build/llama-host --target mukei_llama_native_smoke
          ctest --test-dir build/llama-host --output-on-failure
      - name: Check production inference adapter against pinned capsule
        working-directory: rust
        env:
          MUKEI_LLAMA_NATIVE_LIB_DIR: ${{ github.workspace }}/rust/llama-cpp-prebuilt/prebuilt/host
        run: >-
          cargo check -p mukei-android-jni
          --no-default-features
          --features native_inference

  android-debug:
    needs: publish-pending
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.4.1"
      - name: Test, lint and assemble Android modules
        run: >-
          gradle -p android
          :core:protocol:testDebugUnitTest
          :core:native:testDebugUnitTest
          :app:lintDebug
          :app:assembleDebug
          :app:assembleOffline
          --stacktrace
      - name: Verify offline APK removes INTERNET
        shell: bash
        run: |
          set -euo pipefail
          apk="$(find android/app/build/outputs/apk/offline -type f -name '*.apk' | head -n 1)"
          test -n "$apk"
          if apkanalyzer manifest permissions "$apk" | grep -q 'android.permission.INTERNET'; then
            echo "offline APK unexpectedly declares INTERNET" >&2
            exit 1
          fi

  publish-status:
    if: always()
    needs: [rust-security, android-debug]
    runs-on: ubuntu-latest
    steps:
      - name: Publish final Android CI status
        uses: actions/github-script@v7
        with:
          script: |
            const passed = '${{ needs.rust-security.result }}' === 'success' && '${{ needs.android-debug.result }}' === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: passed ? 'success' : 'failure',
              context: 'android-kotlin-ci',
              description: passed ? 'Android and native validation passed' : 'Android or native validation failed',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
            if (!passed) core.setFailed('Android or native validation failed');
