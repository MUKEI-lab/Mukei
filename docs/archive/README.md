# Documentation Archive

This directory holds historical narrative documents that are useful for provenance,
merge history, and major implementation phases but are not part of the active
documentation surface.

Active, current-state documentation stays in `docs/`.

Archived material here is retained when it adds unique historical context that is
 not already captured by the active docs or straightforwardly recoverable from the
repository structure alone.
